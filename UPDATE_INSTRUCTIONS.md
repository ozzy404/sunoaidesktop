# 🔄 Інструкції з оновлення

## Проблема
Програма показує "API request failed" після авторизації через Google.

## Причина
На вашому комп'ютері залишився **старий файл `src/main.js`** який використовує застарілий метод авторизації через внутрішнє вікно Electron.

## Рішення

### Варіант 1: Завантажити оновлений код з GitHub

1. Відкрийте репозиторій на GitHub
2. Натисніть **Code** → **Download ZIP**
3. Розпакуйте архів
4. Скопіюйте файл `src/main.js` до вашої папки `E:\ATester\sunoaidesktop-main\sunoaidesktop\src\`
5. Замініть старий файл
6. Перезапустіть програму: `npm start`

### Варіант 2: Оновити вручну через Git

```bash
cd E:\ATester\sunoaidesktop-main\sunoaidesktop
git pull origin main
npm start
```

### Варіант 3: Скопіювати файл з робочої області VS Code

1. У VS Code відкрийте файл `/workspaces/sunoaidesktop/src/main.js`
2. Натисніть **Ctrl+A** → **Ctrl+C** (скопіювати весь вміст)
3. Відкрийте файл `E:\ATester\sunoaidesktop-main\sunoaidesktop\src\main.js` у блокноті
4. Натисніть **Ctrl+A** → **Ctrl+V** → **Ctrl+S** (замінити і зберегти)
5. Перезапустіть програму

---

## Як має працювати після оновлення

1. Натискаєте **"Увійти через Google"**
2. Відкривається **ваш системний браузер** (Chrome/Firefox/Edge) з сторінкою Suno
3. Логінитесь через свій Google акаунт **у звичному браузері**
4. З'являється вікно з інструкцією як скопіювати cookie:
   - Натисніть **F12** в браузері
   - Вкладка **Application** → **Cookies** → **https://suno.com**
   - Скопіюйте значення cookie **`__session`**
   - Вставте у поле в програмі
5. Натисніть **"Авторизуватися"**
6. Готово! Ваші треки завантажаться

---

## Перевірка що у вас правильна версія

Після оновлення запустіть програму і подивіться у консоль. Має бути:

```
Opening system browser for Suno login...
Cookie submitted, length: 1234
Session cookie saved successfully
```

**НЕ** має бути:
```
Navigation URL: https://accounts.suno.com/...
OAuth in progress...
Cookie error: Failed to set cookie with an invalid domain attribute
```

Якщо бачите другий варіант - файл не оновився, спробуйте інший спосіб.

---

## Підтримка

Якщо виникли проблеми:
1. Перевірте що файл `src/main.js` оновлено (розмір ~11KB, містить функцію `showCookieInputWindow`)
2. Видаліть папку `node_modules` і запустіть `npm install` заново
3. Очистіть кеш: видаліть папку `%APPDATA%\suno-desktop-player` (Windows)
